import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'popuplive.net',
  appName: 'popuplive-attendy-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
